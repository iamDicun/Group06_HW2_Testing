# Bảng Test Cases — FR-07: Thêm sản phẩm vào giỏ hàng (`POST /api/cart`)

## Danh sách Test Cases

| STT | Test Case ID | Mô tả Test Case & Lý do Phân loại | Feature Postman & Lý do Sử dụng | Input Payload / Header | Expected Result | Đánh giá (Valid/Invalid/Incomplete) | Lý do đánh giá |
|---|---|---|---|---|---|---|---|
| 1 | TC_FR07_01 | **Domain Partition:** Thêm sản phẩm hợp lệ (quantity = 1 integer dương). Phân vùng số nguyên dương hợp lệ. | Collection Runner & Data-Driven: Chạy tự động qua file JSON dữ liệu test | Header: `Bearer <token>` <br> Body: `{"id": 1, "name": "Áo thun", "price": 100000, "quantity": 1}` | Status 200 OK, sản phẩm được thêm vào giỏ | Valid | Test case cơ bản (happy path) cần thiết để xác nhận luồng thêm giỏ hàng hoạt động đúng, đúng tính chất Domain Partitions |
| 2 | TC_FR07_02 | **Domain Partition:** quantity = 0. Biên dưới của phân vùng không hợp lệ. | Data-Driven Runs: Đọc biến `quantity: 0` từ file data | Header: `Bearer <token>` <br> Body: `{"id": 1, "name": "Áo thun", "price": 100000, "quantity": 0}` | Status 400 Bad Request, thông báo lỗi số lượng không hợp lệ | Valid | Negative test case cần thiết, kiểm tra đúng giá trị biên (boundary value) của phân vùng số lượng |
| 3 | TC_FR07_03 | **Domain Partition:** quantity = -5 (số âm). Phân vùng giá trị âm không hợp lệ. | Data-Driven Runs: Đọc biến `quantity: -5` từ data file | Header: `Bearer <token>` <br> Body: `{"id": 1, "name": "Áo thun", "price": 100000, "quantity": -5}` | Status 400 Bad Request | Valid | Negative test case cần thiết, đúng tính chất Domain Partitions, kỳ vọng lỗi trả về status code 4xx |
| 4 | TC_FR07_04 | **Domain Partition:** quantity = 1.5 (số thực). Phân vùng sai kiểu dữ liệu (chỉ nhận số nguyên). | Data-Driven Runs: Kiểm thử kiểu dữ liệu từ data file | Header: `Bearer <token>` <br> Body: `{"id": 1, "name": "Áo thun", "price": 100000, "quantity": 1.5}` | Status 400 Bad Request | Valid | Negative test case cần thiết để xác nhận backend từ chối sai kiểu dữ liệu (integer vs float) |
| 5 | TC_FR07_05 | **Domain Partition:** quantity = "hai" (chuỗi). Phân vùng sai kiểu dữ liệu string. | Data-Driven Runs: Đọc giá trị dạng string | Header: `Bearer <token>` <br> Body: `{"id": 1, "name": "Áo thun", "price": 100000, "quantity": "hai"}` | Status 400 Bad Request | Valid | Negative test case cần thiết, đúng tính chất Domain Partitions cho kiểu dữ liệu sai |
| 6 | TC_FR07_06 | **Domain Partition:** Thiếu trường quantity. Kiểm thử điều kiện trường bắt buộc. | Collection Tests Script: Validate tham số đầu vào | Header: `Bearer <token>` <br> Body: `{"id": 1, "name": "Áo thun", "price": 100000}` | Status 400 Bad Request | Valid | Cần thiết để đảm bảo API validate đầy đủ trường bắt buộc (required field) |
| 7 | TC_FR07_07 | **Domain Partition:** quantity cực lớn (999999). Kiểm thử phân vùng giới hạn quá tải số lượng. | Data-Driven Runs: Truyền giá trị cận trên cực đại | Header: `Bearer <token>` <br> Body: `{"id": 1, "name": "Áo thun", "price": 100000, "quantity": 999999}` | Status 400 Bad Request hoặc giới hạn tồn kho | Valid | Cần thiết để kiểm tra hệ thống có giới hạn tồn kho (inventory cap) hợp lý, tránh đặt hàng vượt khả năng cung ứng |
| 8 | TC_FR07_08 | **Domain Partition:** price hợp lệ (> 0). Phân vùng giá hợp lệ. | Environment Variables: Lấy `base_url` và `token` | Header: `Bearer <token>` <br> Body: `{"id": 1, "name": "Áo thun", "price": 150000, "quantity": 1}` | Status 200 OK | Valid | Đúng tính chất Domain Partitions, xác nhận luồng giá hợp lệ hoạt động bình thường |
| 9 | TC_FR07_09 | **Domain Partition:** price = 0. Biên phân vùng giá không hợp lệ (giá trị không dương). | Data-Driven Runs: Đọc giá trị `price: 0` | Header: `Bearer <token>` <br> Body: `{"id": 1, "name": "Áo thun", "price": 0, "quantity": 1}` | Status 400 Bad Request | Valid | Negative test case cần thiết, kiểm tra đúng giá trị biên của phân vùng giá |
| 10 | TC_FR07_10 | **Domain Partition:** price = -100000. Phân vùng giá âm không hợp lệ. | Data-Driven Runs: Đọc giá trị `price: -100000` | Header: `Bearer <token>` <br> Body: `{"id": 1, "name": "Áo thun", "price": -100000, "quantity": 1}` | Status 400 Bad Request | Valid | Negative test case cần thiết, đúng tính chất Domain Partitions cho giá trị âm |
| 11 | TC_FR07_11 | **Domain Partition:** price = "100k" (chuỗi). Phân vùng sai kiểu dữ liệu số. | Data-Driven Runs: Kiểm thử kiểu dữ liệu invalid | Header: `Bearer <token>` <br> Body: `{"id": 1, "name": "Áo thun", "price": "100k", "quantity": 1}` | Status 400 Bad Request | Valid | Cần thiết để xác nhận backend không ép kiểu string thành number một cách không kiểm soát |
| 12 | TC_FR07_12 | **Domain Partition:** id tồn tại trong hệ thống. Phân vùng ID hợp lệ. | Collection Variables: Lấy ID sản phẩm hợp lệ | Header: `Bearer <token>` <br> Body: `{"id": 1, "name": "Áo thun", "price": 100000, "quantity": 1}` | Status 200 OK | Valid | Test case cơ bản, xác nhận ID hợp lệ được xử lý đúng |
| 13 | TC_FR07_13 | **Domain Partition:** id không tồn tại (999999). Phân vùng entity không tìm thấy. | Data-Driven Runs: Đọc ID không có trong CSDL | Header: `Bearer <token>` <br> Body: `{"id": 999999, "name": "SP Giả", "price": 100000, "quantity": 1}` | Status 404 Not Found | Valid | Negative test case cần thiết để đảm bảo hệ thống không cho thêm sản phẩm không tồn tại vào giỏ |
| 14 | TC_FR07_14 | **Domain Partition:** id = "abc" (chuỗi). Phân vùng sai kiểu dữ liệu ID (yêu cầu số). | Data-Driven Runs: Đọc ID sai format | Header: `Bearer <token>` <br> Body: `{"id": "abc", "name": "Áo thun", "price": 100000, "quantity": 1}` | Status 400 Bad Request | Valid | Negative test case cần thiết, đúng tính chất Domain Partitions cho kiểu dữ liệu ID sai |
| 15 | TC_FR07_15 | **Domain Partition:** name hợp lệ chứa ký tự Unicode ("Áo thun Nam"). | Environment Variables: Đọc token người dùng | Header: `Bearer <token>` <br> Body: `{"id": 1, "name": "Áo thun Nam", "price": 100000, "quantity": 1}` | Status 200 OK | Valid | Cần thiết để đảm bảo hệ thống hỗ trợ đầy đủ tiếng Việt có dấu (Unicode) |
| 16 | TC_FR07_16 | **Domain Partition:** name rỗng "". Biên phân vùng chuỗi rỗng. | Data-Driven Runs: Đọc name rỗng từ file data | Header: `Bearer <token>` <br> Body: `{"id": 1, "name": "", "price": 100000, "quantity": 1}` | Status 400 Bad Request | Valid | Negative test case cần thiết, kiểm tra đúng giá trị biên chuỗi rỗng của phân vùng name |
| 17 | TC_FR07_17 | **State Transition:** Chuyển trạng thái giỏ hàng từ Rỗng → Có 1 item. | Collection Test Script: Kiểm tra trạng thái giỏ hàng trước và sau khi thực hiện request | Header: `Bearer <token>` <br> Body: `{"id": 1, "name": "Áo thun", "price": 100000, "quantity": 1}` | Giỏ hàng thêm sản phẩm thành công, tổng item tăng từ 0 lên 1 | Valid | Cần thiết để xác nhận đúng sự chuyển đổi trạng thái tài nguyên giỏ hàng (State Transition) |
| 18 | TC_FR07_18 | **State Transition:** Thêm sản phẩm đã có sẵn trong giỏ → Cập nhật cộng dồn quantity, không tạo dòng mới. | Postman Pre-request Script & Chain Requests: Gọi thêm sản phẩm 2 lần liên tiếp | Header: `Bearer <token>` <br> Body: `{"id": 1, "name": "Áo thun", "price": 100000, "quantity": 2}` | Số lượng sản phẩm ID 1 trong giỏ tăng dồn, số lượng dòng không thay đổi | Valid | Cần thiết để đảm bảo logic cộng dồn số lượng (merge) hoạt động đúng, tránh trùng lặp dòng dữ liệu |
| 19 | TC_FR07_19 | **State Transition:** Thêm sản phẩm khác ID → Tạo dòng item mới trong giỏ hàng. | Collection Runner: Chạy dãy request nối tiếp | Header: `Bearer <token>` <br> Body: `{"id": 2, "name": "Quần jeans", "price": 200000, "quantity": 1}` | Tạo thêm 1 phần tử riêng biệt trong danh sách giỏ hàng | Valid | Cần thiết để xác nhận giỏ hàng phân biệt đúng các sản phẩm khác ID |
| 20 | TC_FR07_20 | **State Transition:** Đồng bộ trạng thái giỏ hàng giữa các phiên đăng nhập (Token persistence). | Workspaces & Environment Variables: Lưu token vào Environment và gọi API `GET /api/cart` | Header: `Bearer <token>` <br> Method: `GET /api/cart` sau khi POST | Giỏ hàng phản ánh chính xác các sản phẩm vừa thêm | Valid | Cần thiết để đảm bảo dữ liệu giỏ hàng được lưu trữ và đồng bộ đúng theo phiên người dùng |
| 21 | TC_FR07_21 | **Security (SEC-02):** Không gửi Header Authorization (JWT). Kiểm tra yêu cầu xác thực API. | Environment Variables: Xóa biến `{{user_token}}` để test unauthenticated | Header: (Không truyền token) <br> Body: `{"id": 1, "name": "Áo thun", "price": 100000, "quantity": 1}` | Status 401 Unauthorized | Valid | Kiểm tra bắt buộc xác thực là cần thiết, vì thêm giỏ hàng gắn với tài khoản người dùng cụ thể |
| 22 | TC_FR07_22 | **Security (SEC-02):** Gửi Token JWT không hợp lệ hoặc đã hết hạn. Kiểm tra tính an toàn Token. | Collection Runner: Đọc `invalid_token` từ file dữ liệu | Header: `Authorization: Bearer invalid_jwt_abc123` <br> Body: Valid Body | Status 401 Unauthorized / 403 Forbidden | Valid | Cần thiết để kiểm tra hệ thống xử lý đúng token không hợp lệ/hết hạn, tránh chiếm quyền trái phép |
| 23 | TC_FR07_23 | **Security (SEC-02):** Gửi Token sai định dạng (không có từ khóa Bearer). | Data-Driven Runs: Đọc token sai format `token_without_bearer` | Header: `Authorization: 1234567890` <br> Body: Valid Body | Status 401 Unauthorized | Valid | Cần thiết để đảm bảo middleware xác thực kiểm tra đúng định dạng chuẩn Bearer |
| 24 | TC_FR07_24 | **Security (SEC-05):** SQL Injection trong tham số id (`1; DROP TABLE cart;--`). Kiểm tra Parameterized Query. | Data-Driven Runs: Đọc SQLi vector từ file JSON | Header: `Bearer <token>` <br> Body: `{"id": "1; DROP TABLE cart;--", "name": "Áo", "price": 1000, "quantity": 1}` | Status 400 Bad Request, không gây lỗi cơ sở dữ liệu | Valid | Kiểm tra liên quan đến SQL Injection là cần thiết để đảm bảo CSDL an toàn |
| 25 | TC_FR07_25 | **Security (SEC-05):** SQL Injection trong trường name (`' OR '1'='1`). | Data-Driven Runs: Đọc chuỗi SQLi từ file data | Header: `Bearer <token>` <br> Body: `{"id": 1, "name": "' OR '1'='1", "price": 100000, "quantity": 1}` | Dữ liệu được escape an toàn, không bị lỗi SQL | Valid | Cần thiết để đảm bảo trường name được escape/parameterize đúng, tránh SQL Injection |
| 26 | TC_FR07_26 | **Security (SEC-04):** Stored XSS trong trường name (`<script>alert(1)</script>`). Kiểm tra escaping UI/API. | Data-Driven Runs: Đọc XSS payload từ file test | Header: `Bearer <token>` <br> Body: `{"id": 1, "name": "<script>alert(1)</script>", "price": 100000, "quantity": 1}` | Chuỗi được escape an toàn khi lưu trữ và phản hồi | Valid | Cần thiết cho việc kiểm tra XSS, tránh script độc hại bị lưu và thực thi lại ở phía client |
| 27 | TC_FR07_27 | **Security (Parameter Tampering / Price Manipulation):** Client cố tình truyền price thấp hơn giá trong DB (truyền giá 100đ thay vì 100,000đ). | Mock Server / Dynamic Scripting: So sánh phản hồi giá hệ thống với giá client gửi | Header: `Bearer <token>` <br> Body: `{"id": 1, "name": "Áo thun", "price": 100, "quantity": 1}` | Hệ thống tự lấy giá từ CSDL hoặc từ chối giá cố tình thay đổi từ Client | Valid | Rất cần thiết, đây là lỗ hổng nghiêm trọng (business logic flaw) có thể gây thất thoát tài chính trực tiếp nếu không kiểm tra |
| 28 | TC_FR07_28 | **Schema Validation:** Phản hồi thành công có cấu trúc JSON đúng chuẩn và đủ thuộc tính. | Postman Assertion Scripts (`pm.response.to.have.jsonSchema`): Kiểm tra cấu trúc Schema JSON | Header: `Bearer <token>` <br> Body: Valid Body | Phản hồi trả về đúng Schema (chứa id, quantity, v.v.) | Valid | Cần thiết để đảm bảo hợp đồng dữ liệu (data contract) giữa API và Client không bị phá vỡ |
| 29 | TC_FR07_29 | **Schema Validation:** Kiểm tra Header Content-Type của Phản hồi trả về phải là `application/json`. | Postman Assertion Scripts (`pm.response.to.have.header`): Kiểm tra Header | Header: `Bearer <token>` <br> Body: Valid Body | Header `Content-Type` chứa `application/json` | Valid | Kiểm tra phản hồi đúng chuẩn RESTful là cần thiết |
| 30 | TC_FR07_30 | **Schema Validation:** Gửi Request Body rỗng `{}`. Kiểm tra xử lý Schema lỗi. | Collection Runner: Gửi payload `{}` | Header: `Bearer <token>` <br> Body: `{}` | Status 400 Bad Request với Schema lỗi chuẩn | Valid | Cần thiết để xác nhận API validate đầy đủ khi thiếu toàn bộ dữ liệu bắt buộc |
| 31 | TC_FR07_31 | **Schema Validation:** Cú pháp JSON bị lỗi (`{"id": 1, "quantity":}`). | Collection Runner: Truyền JSON malformed | Header: `Bearer <token>` <br> Body: Cú pháp JSON sai | Status 400 Bad Request (Invalid JSON Syntax) | Valid | Cần thiết để đảm bảo server xử lý gracefully khi nhận JSON không hợp lệ, tránh crash |
| 32 | TC_FR07_32 | **Schema Validation:** Gửi thêm trường dư thừa không định nghĩa (`"unknown_field": "test"`). | Data-Driven Runs: Đọc JSON chứa extra property | Header: `Bearer <token>` <br> Body: `{"id": 1, "name": "Áo", "price": 10, "quantity": 1, "extra": "abc"}` | Hệ thống bỏ qua trường dư thừa hoặc báo 400 Bad Request | Valid | Cần thiết để xác nhận hệ thống xử lý nhất quán với các trường không nằm trong schema |
| 33 | TC_FR07_33 | **Domain Partition:** Ký tự đặc biệt UTF-8 trong tên sản phẩm ("Áo thun 👕"). Phân vùng Encoding. | Data-Driven Runs: Đọc chuỗi Emoji Unicode | Header: `Bearer <token>` <br> Body: `{"id": 1, "name": "Áo thun 👕", "price": 100000, "quantity": 1}` | Status 200 OK, lưu trữ đúng chuỗi UTF-8 | Valid | Cần thiết để đảm bảo hệ thống hỗ trợ đầy đủ bảng mã UTF-8, bao gồm cả emoji |
| 34 | TC_FR07_34 | **State Transition:** Thêm sản phẩm vào giỏ, sau đó gọi Checkout (`POST /api/checkout`) → Giỏ hàng phải được làm rỗng. | Postman Workflow (Chaining Requests): Gọi lần lượt `POST /api/cart` → `POST /api/checkout` → `GET /api/cart` | Chuỗi các lệnh API tương ứng | Giỏ hàng trống sau khi đặt hàng thành công | Valid | Cần thiết để đảm bảo vòng đời (lifecycle) đầy đủ của giỏ hàng, tránh đặt hàng trùng lặp lần sau |
| 35 | TC_FR07_35 | **Schema Validation / Performance SLA:** Thời gian phản hồi của API `POST /api/cart` phải ≤ 500ms. | Postman Monitor & Test Script (`pm.expect(pm.response.responseTime).to.be.below(500)`): Đo latency | Body: Valid Body | API phản hồi nhanh dưới 500ms. | Valid | Cần thiết để đảm bảo hiệu năng API đáp ứng SLA, tránh trải nghiệm chậm khi giỏ hàng có tải lớn |


