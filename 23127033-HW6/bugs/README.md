# Bug Reports — HW06 API Testing

Thư mục này lưu trữ các hồ sơ báo lỗi thực tế (Genuine Bugs) phát hiện được trong backend `eshop-sut` khi thực thi bộ test Postman/Newman.

## Danh sách lỗi:
* `BUG-API-01-xxxx.md`: (Sẽ cập nhật sau khi chạy test)
* `BUG-API-02-xxxx.md`: (Sẽ cập nhật sau khi chạy test)
